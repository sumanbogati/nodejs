As the particular example needs the socket.io package to run that example. 
For run this example you need to download socket.io under the root folder, 
so you can run this command from root folder for download this package. 

//////////////////////////
npm install socket.io
//////////////////////////